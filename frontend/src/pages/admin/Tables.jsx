/**
 * Admin Tables page — wraps the shared Tables component that already
 * has full floor/table CRUD with real backend calls.
 */
import TablesManager from '../../components/Tables';

const Tables = () => {
  return <TablesManager />;
};

export default Tables;
